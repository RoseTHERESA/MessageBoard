Welcome to messageBoard!

<% if user_signed_in? %>
	<div>Hello <%= current_user.email %>! Would you like to 
	<%= link_to("Sign Out?", :destroy_user_session, method: :delete) %></div>
<% else %>
	<a href="/users/sign_up">Sign Up!</a>
	<a href="/users/sign_in">Sign In!</a>
<% end %>